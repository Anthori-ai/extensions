# Agent Foundation

First-party project extension that contributes hidden shared Anthori agent foundations:

- a hidden `agent-base` control definition over the built-in runtime host
- a hidden `text-provider-base` control definition that first-party text-provider extensions specialize

This package is intended to be used as a dependency by agent/provider ecosystems rather than as a user-facing package.
