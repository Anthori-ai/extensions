format: 1
id: simple-provider-runtime
name: Simple Provider Runtime
description: Runtime library that forwards generic text-provider requests to a configured HTTP endpoint.
supportedOs:
  - windows
  - mac
  - linux
entry: runtime/simple-provider-runtime.js
actions:
  - id: list-models
    name: List Models
    description: Return the configured Simple HTTP model and its required context limit.
  - id: respond-text
    name: Respond Text
    description: Send a generic text request to the configured HTTP endpoint.
