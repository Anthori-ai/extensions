format: 1
id: delete-conduit
name: Delete
description: Deletes a conduit from the current graph.
baseControlRef: controls/1/extensions/anthori.app.graph/definitions/graph-base
contracts:
  caller:
    contracts:
      - id: default
        name: Delete
        description: Delete a conduit from the current graph.
        input:
          type: object
          description: Conduit identifier to remove.
          additionalProperties: false
          properties:
            id:
              type: string
              description: Conduit id to delete.
          required:
            - id
        output:
          type: void
          description: No structured result.
control:
  visible: true
  libraryPath: app/project/graph/conduit
  configSchema:
    runtimeActionForward:
      type: string
      default: delete-conduit
