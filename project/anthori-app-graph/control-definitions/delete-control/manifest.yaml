format: 1
id: delete-control
name: Delete
description: Deletes a control from the current graph.
baseControlRef: controls/1/extensions/anthori.app.graph/definitions/graph-base
contracts:
  caller:
    contracts:
      - id: default
        name: Delete
        description: Delete a control from the current graph.
        input:
          type: object
          description: Control identifier to remove.
          additionalProperties: false
          properties:
            id:
              type: string
              description: Control id to delete.
          required:
            - id
        output:
          type: void
          description: No structured result.
control:
  visible: true
  libraryPath: app/project/graph/control
  configSchema:
    runtimeActionForward:
      type: string
      default: delete-control
