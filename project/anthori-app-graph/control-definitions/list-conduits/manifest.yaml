format: 1
id: list-conduits
name: List
description: Lists all conduits in the current graph.
baseControlRef: controls/1/extensions/anthori.app.graph/definitions/graph-base
contracts:
  caller:
    contracts:
      - id: default
        name: List
        description: List conduits in the current graph.
        input:
          type: void
          description: No input.
        output:
          type: array
          description: Conduits in the current graph.
          items:
            type: object
            additionalProperties: false
            required:
              - id
              - fromControlId
              - fromControlPortId
              - toControlId
            properties:
              id:
                type: string
                description: Conduit id.
              fromControlId:
                type: string
                description: Source control id.
              fromControlPortId:
                type: string
                description: Source port id.
              toControlId:
                type: string
                description: Target control id.
control:
  visible: true
  libraryPath: app/project/graph/conduit
  configSchema:
    runtimeActionForward:
      type: string
      default: list-conduits
