format: 1
id: list
name: List
description: Lists all projects in the user's workspace.
baseControlRef: controls/1/extensions/anthori.app.project/definitions/project-base
contracts:
  caller:
    contracts:
      - id: default
        name: List
        description: Lists all projects in the user's workspace.
        input:
          type: void
          description: No input required.
        output:
          type: array
          description: Projects visible in the current workspace.
          items:
            type: object
            additionalProperties: false
            required:
              - id
              - name
              - description
              - graphId
            propertyOrder:
              - id
              - name
              - description
              - graphId
            properties:
              id:
                type: string
                description: Stable project id.
              name:
                type: string
                description: Display name shown in the project list.
              description:
                type: string
                description: Saved project description text.
              graphId:
                type: string
                description: Main graph id for the project.
control:
  visible: true
  configSchema:
    runtimeActionForward:
      type: string
      default: list
