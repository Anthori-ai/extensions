bundle:
  namespace: anthori.app.session
  owner: Anthori
  locale: en-US
  version: 1
entries:
  - key: app.session.start.sessionId.help
    kind: field_tip
    target: app.session.start.sessionId
    format: text
    text: Optional. Leave blank to start a transient child session.
