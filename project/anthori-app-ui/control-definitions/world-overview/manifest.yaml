format: 1
id: world-overview
name: World Overview
description: Summarize open Anthori windows, panels, modals, and pointer ownership.
baseControlRef: controls/1/extensions/anthori.app.ui/definitions/ui-automation-base
contracts:
  caller:
    contracts:
      - id: default
        name: World Overview
        description: Summarize open Anthori windows, panels, modals, and pointer ownership.
        input:
          type: object
          description: No input.
        output:
          type: object
          required:
            - result
          properties:
            result:
              description: Result returned by the desktop UI host.

control:
  visible: true
  configSchema:
    runtimeActionForward:
      type: string
      default: worldOverview
