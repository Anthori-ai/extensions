# Signals Standard

First-party signal controls packaged as a project extension.

- Uses the generic executable host through `anthori-signals-standard-runtime`
- Contributes visible `Signals > Alarm`, `Signals > Timer`, and `Signals > Shutdown` controls
- Leaves `Channel` in the kernel as the queue-backed runtime transport signal
