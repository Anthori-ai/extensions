# Display

First-party display and input controls packaged as a project extension.

- Uses the generic executable host through `anthori-display-runtime`
- Currently supported on macOS only
- Surfaces explicit `Display` folder controls such as `List`, `Screenshot`, `Type`, and mouse/keyboard helpers
