format: 1
id: delete
name: Delete
description: Deletes files or directories inside the project workspace.
baseControlRef: controls/1/extensions/anthori.system.filesystem/definitions/filesystem-base
contracts:
  caller:
    contracts:
      - id: default
        name: Delete
        description: Delete a file or directory from the project workspace.
        input:
          type: object
          description: Delete request.
          required:
            - path
          properties:
            path:
              type: string
              description: Path relative to the project workspace, or an absolute path.
            recursive:
              type: boolean
              description: Required when deleting directories.
          additionalProperties: false
        output:
          type: void
          description: No structured result. Check details for delete metadata when requested.
control:
  visible: true
  configSchema:
    runtimeActionForward:
      type: string
      default: delete
