format: 1
id: rename
name: Rename
description: Renames or moves files and directories inside the project workspace.
baseControlRef: controls/1/extensions/anthori.system.filesystem/definitions/filesystem-base
contracts:
  caller:
    contracts:
      - id: default
        name: Rename
        description: Rename or move a file or directory to a new workspace path.
        input:
          type: object
          description: Rename or move request.
          required:
            - source
            - destination
          properties:
            source:
              type: string
              description: Source path relative to the project workspace, or an absolute path.
            destination:
              type: string
              description: Destination path relative to the project workspace, or an absolute path.
          additionalProperties: false
        output:
          type: void
          description: No structured result. Check details for rename metadata when requested.
control:
  visible: true
  configSchema:
    runtimeActionForward:
      type: string
      default: rename
