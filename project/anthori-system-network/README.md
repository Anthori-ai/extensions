# Network

First-party network controls packaged as a project extension.

- Uses the generic executable host through `anthori-network-runtime`
- Currently exposes the `System > Network > Fetch` control
- Provides direct HTTP requests for project graphs without tying them to browser automation
